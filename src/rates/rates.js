angular.module('pmp/rates', [
    'pmp/data',
    'ngHandsontable',
])

.directive('rates', function() {
    return {
        templateUrl: 'rates/

.controller('RatesCtrl', function(Rates) {
    this.Rates = Rates;
});
